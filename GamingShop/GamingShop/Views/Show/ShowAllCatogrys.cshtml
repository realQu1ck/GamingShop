
@{
    ViewData["Title"] = "ShowAllCatogrys";
}
@model List<TblCategory>

<div class="form-group" style="padding:30px">
    @foreach (var item in Model)
    {
        <h4>@item.Name</h4>
    }
</div>

